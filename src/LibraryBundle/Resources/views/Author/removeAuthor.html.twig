<form action="{{ path('library_remove_author', {'slug': slug}) }}" method="post">
	<button data-toggle="tooltip" data-placement="top" title="Supprimer" type="submit" class="btn btn-danger" onclick="return confirm('Etes vous sûr de vouloir supprimer cet auteur et tous les livres et prêts associés ? ?')"><i class="fa fa-minus-circle"></i> Supprimer cet auteur</button>
	{{ form_rest(form) }}
</form>