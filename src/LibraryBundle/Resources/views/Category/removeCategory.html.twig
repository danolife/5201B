<form action="{{ path('library_remove_category', {'slug': slug }) }}" method="post" style="display: inline-block">
    <button data-toggle="tooltip" data-placement="top" title="Supprimer" type="submit" class="btn-link" onclick="return confirm('Etes vous sûr de vouloir supprimer cette catégorie et tous les livres et prêts associés ?')"><i class='text-danger fa fa-trash'></i></button>
    {{ form_rest(form) }}
</form>

