{% extends "LibraryBundle:Default:layout.html.twig" %}

{% block body %}
	<h2>Catégorie : {{ category.name }}</h2>
	{% for book in books %}
		<a href="{{ path('library_book', {'slug': book.slug}) }}">{{ book.title }}</a> by <a href="{{ path('library_author', {'slug': book.author.slug}) }}">{{ book.author.name }}</a>
	{% endfor %}
{% endblock %}