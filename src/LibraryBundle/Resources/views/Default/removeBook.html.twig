<form action="{{ path('library_remove_book', {'slug': slug }) }}" method="post">
    <button type="submit" class="btn btn-danger" onclick="return confirm('Etes vous sûr de vouoir supprimer ce livre ?')"><i class='fa fa-minus-circle'></i> Supprimer ce livre</button>
    {{ form_rest(form) }}
</form>

